import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        padding: 16,
        borderRadius: 12,
        backgroundColor: "#fff",
        marginBottom: 12,
    },
    title: {
        fontSize: 16,
        fontWeight: "600",
    },
    year: {
        marginTop: 4,
        color: "#999",
    },
});